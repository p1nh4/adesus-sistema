name: Deploy PRD

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: Deploy
        run: |
          cd /srv/adesus &&
          git pull &&
          composer install --no-interaction &&
          php artisan migrate --force &&
          php artisan config:clear
